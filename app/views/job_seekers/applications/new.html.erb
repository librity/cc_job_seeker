<% provide(:resource_path, job_seekers_job_applications_path(@job)) %>
<% provide(:method, :post) %>

<main class='row'>
  <div class='col-md-12'>
    <header class='header'>
      <h1><%= t('views.job_seekers/applications.create') %></h1>

      <p><%= t('views.job_seekers/applications.context', job_title: @job.title) %></p>
    </header>

    <section class='contents'>
      <%= render 'form' %>
    </section>

  </div>
</main>
